# E3 — Restauração do canal Evolution da Camila (gate humano)

**Data:** 2026-08-09  
**Branch:** `ops/camila-channel-e3`  
**Pré-requisitos mergeados:** #152 (E1 webhook auth), #153 (E2 canário), #154 (persona transparente)  
**Instância operacional:** `prospec-ativa-camila2`  
**Produto (BDR):** `806b5975-e268-402e-a65c-9e9503271041`  
**Agente Camila:** `68aeece9-26f2-4f7b-a595-a6ea5e8acfa7`

## Check binário (só fecha com APROVO + evidência live)

E3 só fecha quando **todas** forem verdadeiras:

1. `prospec-ativa-camila2` conectada em **duas leituras** com ≥30 min entre elas (`connection_status`/`state` = open/connected).
2. Round-trip real: outbound WhatsApp → inbound resposta, com IDs correlacionados (Evolution message id / wamid ↔ linha em `platform_crm_message_events` ou equivalente).
3. Webhook live `platform-evolution-webhook` responde **2xx** no evento do round-trip (não 401).
4. Dry-run / só diagnóstico **não** fecha E3.

## Gate humano obrigatório

**Antes** de QR, `webhook/set`, `subscribe_webhook`, reconnect, ou qualquer mensagem real WhatsApp, Marcelo precisa escrever textual:

`APROVO E3` + janela de manutenção (horário).

Sem essa frase: **zero mutação de produção**.

## Estado de código (pré-mutação) — verificado em `51d8c99`

| Item | Evidência |
|------|-----------|
| E1 no HEAD | `deno test` `evolution-webhook-auth.test.ts` → **22 passed** (2026-08-09) |
| Provisioner envia `apikey: instanceToken` | `evolution-webhook-provisioners.ts` L56–65 |
| Receptor rejeita token ausente/errado | testes receptor 401 + sem escrita |
| Receptor processa só instância autenticada | teste + ACK scoped |
| HANDOFF ops | `apps/NexvyBeauty/tasks/HANDOFF-PROSPECCAO-ATIVA-2026-08-07.md` |
| Persona seed | migration `20260809_seed_bdr_camila_transparent_identity.sql` (não aplicar `20260804` depois) |

## Diagnóstico read-only (pode rodar sem APROVO)

Ordem sugerida — só leitura / UI / logs:

1. **UI** Platform CRM → Evolution instances → localizar `prospec-ativa-camila2`.
2. **Status connection** via proxy/UI (sem `connect`/`logout`/`webhook/set`).
3. **SQL read-only** (exemplo):

```sql
-- instância
select name, connection_status, phone_number, updated_at, last_webhook_at
from platform_evolution_instances
where name ilike '%camila%'
order by updated_at desc;

-- eventos recentes (se tabela existir no ambiente)
select created_at, event_type, instance_name, status_code
from platform_crm_message_events
where instance_name = 'prospec-ativa-camila2'
order by created_at desc
limit 20;
```

4. **Logs** Edge `platform-evolution-webhook` (últimas 24h): contar 401 vs 2xx para a instância.
5. Preencher abaixo a seção “Diagnóstico capturado” **sem** secrets (redigir tokens).

### Diagnóstico capturado (preencher na execução)

| Campo | Valor | Capturado em |
|-------|-------|--------------|
| connection_status (leitura 1) | _pendente_ | |
| connection_status (leitura 2, ≥30 min) | _pendente_ | |
| last_webhook_at | _pendente_ | |
| Ratio 401/2xx (24h) | _pendente_ | |
| Root cause candidata | _pendente_ | |

## Procedimento pós-`APROVO E3` (mutações)

Executar **somente** na janela aprovada, na ordem:

1. Confirmar deploy de E1 em produção (functions `platform-evolution-webhook` + proxy alinhados ao commit mergeado).
2. Reconfigurar webhook da instância com `instanceToken` no header (`subscribe_webhook` / `webhook/set` via caminho de produto — não inventar URL).
3. Se desconectada: `connect_instance` / QR pelo painel Platform; aguardar open.
4. Leitura 1 de conexão → esperar ≥30 min → leitura 2.
5. Round-trip controlado (número de teste autorizado): 1 outbound + 1 inbound.
6. Anexar evidência redigida: IDs, timestamps, status HTTP 2xx, print/log sem token.
7. Rollback se falhar: ver seção abaixo; **não** seguir para piloto (próximo stage) sem E3 verde.

### Evidência live (pós-APROVO)

| Check | Resultado | ID / nota redigida |
|-------|-----------|--------------------|
| Conectada leitura 1 | | |
| Conectada leitura 2 (≥30 min) | | |
| Outbound wamid/id | | |
| Inbound correlacionado | | |
| Webhook 2xx | | |

## Rollback

1. Parar novos envios de campanha BDR (throttle / pausar campanha na UI).
2. Se webhook mal configurado: reverter `webhook/set` para URL/token anteriores **só** se conhecidos; senão desabilitar webhook até re-pareamento.
3. Se QR/reconnect instável: `logout` controlado + re-pair em nova janela `APROVO`.
4. Não reaplicar seed `20260804` (persona antiga).

## Fora deste PR

- Aplicar migration `20260809` em prod (gate separado — seed persona).
- Piloto 1–10 leads (stage seguinte, após E3 verde).
- Qualquer envio em massa.

## Declaração deste artefato

Este runbook **não** executa mutação de produção. Fecha apenas a preparação operacional de E3 e o checklist de gate.
