# TODO — Build ADS NexvyBeauty (1+2+3) · /loop oneshot

> **Sessão:** trabalho ADS (branch `feat/ads-nexvybeauty`, worktree isolado `../SaasPlugin_vite-ads`).
> **Controladora:** `local_a3502241` "NexvyBeauty GO-LIVE controller session" — reportar ao fim de CADA camada.
> **Base:** `origin/main` @ 573819c (#82). **Escopo aprovado:** 1+2+3, fast-follow (NÃO gate de go-live).
> **Prioridade:** B (atribuição) > A1 (gestão). Nada crítico.

## Regras do loop (ordem da controladora)
- **BUILD-ONLY**, tudo **gated OFF**. Fact-Forcing + verify-live-before-write.
- **HITL-STOP (parar e chamar a controladora) ANTES de:** deploy prod · migration no live · merge · **chamar API real da Meta** (ads_* MCP tools NÃO com conta real) · flip de flag · **tocar qualquer `demo-*`**.
- **Entrega:** PRs **stacked** (docs · A2-back · Front), padrão #78-81. Deploy/merge/migration = 100% comando da controladora.
- **STOP:** build 100% (deno check/build verde, PRs cortados gated, docs prontos) → reporta e espera.

## Streams

### S0 — Fundação & docs (eu) → PR base `feat/ads-docs`
- [ ] Commitar os 5 blueprints ADS recuperados do stash (não deixar solto).
- [ ] `CHECKLIST-ATIVACAO-ADS-2026-07-16.md`+`.html` — code-pronto vs. OPS do Marcelo (número Salvy+WABA+Página FB+secrets CAPI/META_ADS_*+cron+App Review+audiências).
- [ ] `PLANO-MESTRE-ADS-2026-07-16.md`+`.html` — consolida as 3 camadas + roadmap + estado.
- **Verifica:** arquivos presentes, pareados .md+.html, commit feito (sem push até PR).

### S-A2back — Agente ads-optimize (edges, gated) → PR `feat/ads-optimize-engine` (base: docs)
- [ ] `_shared/ads-optimize-rules.ts` — regras PURAS testáveis (métricas→recomendações). Ex.: pausar ad com spend≥X e 0 conversão em N dias; subir budget com roas≥meta; realocar budget.
- [ ] edge `ads-optimize` — lê `ads_metrics`(+campaigns/adsets/ads) → grava `ads_recommendations` (status pending). super_admin. NÃO muta nada externo. Dedup por (kind,target) aberto.
- [ ] edge `ads-apply-recommendation` — aplica rec **approved** → Graph → `ads_mutations_log`. **Gated `ADS_MUTATIONS_ENABLED=false` → dry-run** (grava mutation com graph_response={dry_run}). super_admin+confirm:true+audit(before/after,applied_by). Token decifrado só em memória (padrão ads-sync/meta-crypto).
- [ ] `config.toml`: entradas das 2 edges (verify_jwt correto). SEM cron (flip é HITL).
- [ ] deno tests (golden das regras + payload apply).
- **Verifica:** `deno check` EXIT 0; `deno test` verde; regras semeadas→recomendação; apply dry-run grava mutations_log sem tocar Graph.

### S-Front — Seção "Anúncios" super_admin (3 abas) → PR `feat/ads-console` (base: A2back)
- [ ] Aba **Atribuição (B, PRIORIDADE)** — observa `ads_attribution` + `ads_capi_events` (funil CTWA: cliques→leads→qualificados→conversões CAPI c/ status dry_run/sent). Read-only.
- [ ] Aba **Campanhas (A1)** — hierarquia conta→campanha→adset→ad + métricas (spend/impr/clicks/ctr/cpc/cpm/conv/cpa/roas), date range. Empty states pré-App-Review.
- [ ] Aba **Recomendações (A2-UI)** — fila `ads_recommendations` pending → aprovar/rejeitar → chama `ads-apply-recommendation` (mostra resultado dry-run) + histórico `ads_mutations_log`.
- [ ] Nav + rota; product-scoped; super_admin. Padrões da casa (`usePlatformAdsConnection` + cast `as never`; NÃO regenerar types.ts).
- **Verifica:** `npm run build`/`tsc -b` verde; navegável com dados semeados; sem `as never` novo fora do padrão.

### S-B4 — Wire #4 `demo_completed` (produtor do funil CAPI) → coordenar
- [ ] **PING controladora ANTES** (toca `demo-*`). Depois: 1 linha no report da esteira → `pcrm_log_journey_event('demo_completed', …)`. Funil vai a 3/6.
- [ ] #5/#6 (checkout/sale via cakto) = só DOCUMENTAR/stub no CHECKLIST. NÃO construir.

## Travado no OPS do Marcelo (só documentar — fila HITL)
número Salvy+WABA · Página FB · secrets CAPI/META_ADS_*/STATE_SIGNING_SECRET · cron capi-send/ads-sync · App Review Meta · audiências CA_ExclusaoLeads/CA_Semente_ICP.

## Report cadence
Ao fim de S0, S-A2back, S-Front, S-B4 → mensagem à controladora com prova (verde/screenshot/deno test) + próximo passo. STOP final aguardando comando de deploy/merge.
