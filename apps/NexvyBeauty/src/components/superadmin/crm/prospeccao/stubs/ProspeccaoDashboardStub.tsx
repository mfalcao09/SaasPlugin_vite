import { ComingSoonSection } from '@/components/admin/ComingSoonSection';

/**
 * STUB — Dashboard de prospecção (Prospecção Ativa).
 *
 * Futuro: funil da base consolidada — total de handles, quebra por origem
 * (extração), quebra por segmento, % com telefone e taxa de conversão
 * (qualificados / total). Os números virão da mesma view
 * `platform_crm_consolidated_leads`. Estrutura no menu já garantida; a
 * visualização a fundo fica para a próxima fase.
 */
export function ProspeccaoDashboardStub() {
  return (
    <ComingSoonSection
      title="Dashboard de prospecção"
      description="Em construção — funil da base: total de @handles, por origem, por segmento, % com telefone e conversão (qualificados / total), tudo lido da view consolidada."
    />
  );
}

export default ProspeccaoDashboardStub;
