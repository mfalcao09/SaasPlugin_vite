# Spike Salvy — copie para docs/salvy/.env.local (gitignored) e preencha.
# A key é criada UMA vez no painel: app.salvy.com.br/settings/api → "Criar chave de API".
# NUNCA cole a key em chat/PR/commit — só neste arquivo local.

SALVY_API_KEY=cole_a_key_aqui

# Só descomente quando decidir provisionar/cancelar linha REAL (billable):
# SALVY_ALLOW_CREATE=1
